# ============================================================
# Frammer Analytics Backend — requirements.txt
# Single file covering both backend/ and nlp_frammer/
# Install with:
#   pip install --only-binary=duckdb,pandas,numpy -r requirements.txt
# ============================================================

# ── Web framework ────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
python-multipart>=0.0.9

# ── Data layer ───────────────────────────────────────────────
# Pin duckdb — must match nlp layer. Use pre-built wheel only
# (compiling from source OOMs on Render build machines).
duckdb==1.1.3
pandas==2.2.3
numpy>=1.26.0,<2.0.0
pyarrow>=16.0.0            # required by pandas .parquet read/write

# ── ETL utilities ────────────────────────────────────────────
python-dotenv>=1.0.0
watchdog>=4.0.0             # file watcher for auto-ETL on CSV change

# ── NLP — Gemini + LangGraph ─────────────────────────────────
google-genai>=0.8.0         # synthesiser.py + sql_generator.py
langchain-core>=0.3.0       # agent.py message types
langchain-google-genai>=2.0.0  # agent.py ChatGoogleGenerativeAI
langgraph>=0.2.0            # agent.py StateGraph + MemorySaver

# ── NLP — Vector store + retrieval ───────────────────────────
chromadb>=0.5.0,<1.0.0             # vector_store.py
sentence-transformers>=3.0.0  # bi-encoder (all-MiniLM-L6-v2)
                               # + cross-encoder (ms-marco-MiniLM-L-6-v2)

# ── NLP — Chart generation ───────────────────────────────────
plotly>=5.22.0
kaleido>=0.2.1              # plotly write_image() → PNG