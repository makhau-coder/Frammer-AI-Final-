# ============================================================
# Frammer Analytics Backend — requirements.txt
# ============================================================

# ── Web framework ────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
python-multipart>=0.0.9

# ── Data layer ───────────────────────────────────────────────
duckdb==1.1.3
pandas==2.2.3
numpy>=1.26.0,<2.0.0
pyarrow>=16.0.0

# ── ETL utilities ────────────────────────────────────────────
python-dotenv>=1.0.0
watchdog>=4.0.0

# ── NLP — Gemini + LangGraph ─────────────────────────────────
google-genai>=0.8.0
langchain-core>=0.3.0
langchain-google-genai>=2.0.0
langgraph>=0.2.0
google-generativeai>=0.5.0

# ── NLP — Vector store + retrieval ───────────────────────────
chromadb>=0.5.0,<1.0.0
# (sentence-transformers REMOVED for Render Free Tier)

# ── NLP — Chart generation ───────────────────────────────────
plotly>=5.22.0
kaleido>=0.2.1